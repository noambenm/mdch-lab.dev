apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: homepage-httproute
  namespace: homepage
spec:
  parentRefs:
    - name: mdch-lab-gateway
      namespace: cilium
  hostnames:
    - "mdch-lab.dev"
  rules:
    - backendRefs:
      - name: homepage
        port: 3000